export default function NavyDots({ style }: { style?: React.CSSProperties }) {
  // eslint-disable-next-line @next/next/no-img-element
  return <img src="/assets/navy-dots.svg" alt="" width={50} height={23} style={style} />;
}
