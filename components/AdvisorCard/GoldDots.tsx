export default function GoldDots({ style }: { style?: React.CSSProperties }) {
  // eslint-disable-next-line @next/next/no-img-element
  return <img src="/assets/gold-dots.svg" alt="" width={50} height={23} style={style} />;
}
